apiVersion: v1
kind: Service
metadata:
  name: discovery-frontend-service
spec:
  selector:
    app: discovery-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer
